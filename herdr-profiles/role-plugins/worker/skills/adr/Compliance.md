# ADR Compliance Rules

## Purpose

This file lists the MUST / MUST NOT rules extracted from all ACCEPTED ADRs.
Use it as the fast-path compliance check before reading individual ADR files.

## Workspace status

The current workspace ADR index contains only **PROPOSED** ADRs, so there are no binding workspace-specific compliance rules yet.
Agents should still read `docs/context/adr/index.md` to confirm whether that has changed.

## Active Rules

<!-- No active ADR compliance rules yet. Add rules here when an ADR is ACCEPTED. -->
